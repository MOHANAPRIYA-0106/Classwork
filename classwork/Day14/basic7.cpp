
#include<iostream>

using namespace std;

int main()

{
	char ch;

	int count = 0;

	cin >> ch;



		if ((ch == 'A') || (ch == 'a') || (ch == 'E') || (ch == 'e') ||
			(ch == 'I') || (ch == 'i') || (ch == 'O') || (ch == 'o') ||
			(ch == 'U') || (ch == 'u')) {

			count++;

		}
		
			cout << "number of vowels:" << endl;
		return 0;

	


}